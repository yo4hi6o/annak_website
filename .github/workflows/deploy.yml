name: Flutter_Web_Deploy

on:
  push:
    branches: 
      - master

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  flutter_build_web:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 1
      - uses: subosito/flutter-action@v1
        with:
          channel: 'stable'
      - run: flutter pub get
      - run: flutter build web
      - name: ssh key generate
        run: echo "$SSH_PRIVATE_KEY" > key && chmod 600 key
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
      - name: rsync deploy
        run: rsync -avuzr -e "ssh -i key -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -p 10022" --delete build/web/ 	sv13075@162.43.116.76:/home/sv13075//annak.xsrv.jp/public_html/


# name: deploy to xserver

# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#         - uses: actions/checkout@v2
#         - uses: borales/actions-yarn@v2.3.0
#           with:
#             cmd: install
#         - uses: borales/actions-yarn@v2.3.0
#           with:
#             cmd: build
#         - name: ssh key generate
#           run: echo "$SSH_PRIVATE_KEY" > key && chmod 600 key
#           env:
#             SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
#         - name: rsync deployments
#           uses: burnett01/rsync-deployments@5.1
#           with:
#             switches: -avz --delete
#             path: dist/
#             remote_path: /home/	annak/annak.xsrv.jp/public_html/
#             remote_host: 	annak.xserver.jp
#             remote_user: 	phrz478073
#             remote_port: 10022
#             remote_key: ${{ secrets.SSH_PRIVATE_KEY }}
